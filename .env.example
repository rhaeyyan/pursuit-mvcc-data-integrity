# Socrata App Token for the NYC Open Data (SODA) API.
# Server-side only: read it inside a Route Handler, never from a client component,
# and never under a browser-exposed variable name (CLAUDE.md rule 3, NFR-2).
# Copy this file to .env and fill in the value locally; .env is gitignored.
SOCRATA_APP_TOKEN=
