# Active SPEC

*(Empty — no task currently in flight. Next queued: the Staten Island pilot panel's chart/UI
half, sequenced after the fallback-banner-wiring SPEC closed so the two don't edit
`src/app/[[...borough]]/page.tsx` concurrently.)*
